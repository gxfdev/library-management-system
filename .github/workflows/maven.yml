name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Prepare project directory
      run: |
        if [ -d "jekins测试项目/backend" ]; then
          cp -r "jekins测试项目/backend" ./backend-src
        else
          cp -r "backend" ./backend-src
        fi

    - name: Run tests with Maven
      working-directory: backend-src
      run: mvn -B test -Dspring.profiles.active=test

    - name: Build package
      working-directory: backend-src
      if: success()
      run: mvn -B package -DskipTests -Dmaven.javadoc.skip=true

    - name: Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: backend-src/target/surefire-reports/
        retention-days: 30

    - name: Update dependency graph
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' && success()
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
      with:
        directory: backend-src
